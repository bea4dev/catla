[package]
name = "catla"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
num_cpus = "1.0"
thiserror = "1.0"
regex = "1.7.0"